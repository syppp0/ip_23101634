10.
  2번 {}

11.
(1)  30!!!!

(2)  11
     12
     13

(3)  20

12.
     연산자 = 는 값을 대입하는 것으로 예를 들어 a=20이라 선언한다면 a를 출력했을 때 나오는 값은 20이다.
     연산자 ==는 좌변과 우변의 값이 같다는 것을 나타낼 때 선언하며, 얘를 들어 10 == 20을 출력했을 때 나오는 값은 false이다.

13.
     if(x>10 && x<20){
          alert(x);
     }

14.
     let i=1;
     while(i<=10){
          if(i%5==0){
               alert(5);
          }
          i++;
     }

15.
     for(let i=10; i>=0; i-=3){
          alert(i);
     }
